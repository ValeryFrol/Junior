package sorting;

import java.util.Comparator;

public class AlphabetComparator implements Comparator<TextToSort> {
@Override
    public int compare(TextToSort o1, TextToSort o2) {
        return 0;
    }
}
